"use client";
import React, { FC } from "react";

// Stubbed Expert component.
// The original component imported `ExpertData` which was removed from the project.
// Keep a minimal component here to avoid build errors while other files are updated.

const Expert: FC = () => {
    return null;
};

export default Expert;